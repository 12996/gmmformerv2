确认：lr 3e-4→2e-4 就是第一刀，当前训练继续，不停、不重启、不叠加 sft。

还有空间，但不能判定超参不匹配一定比文本表征弱更可能，也不能承诺补回 6.7。本轮目标是相对 178.6 改善、争取接近 185.3，不是修 CLIP 视觉、不是 Table 1。

优先级：lr 第一，sft_factor 第二，整组换 loss_factor 排除（不是单变量）。sft 是 TC 聚合温度 softmax(weight/sft_factor)，不是对比损失温度。

S 取早停规则下最佳 SumR。S≤178.6 无收益；178.6<S<185.3 部分恢复；S≥185.3 单 seed 追平/超过 RoBERTa 基线，另表。

下一动作：让 lr=2e-4 那次按 HN≥20、es_cnt>10、最多100轮跑完。现在不启动第二刀。若 S≥185.3 停搜超参；若 S<185.3 只再做一次 sft 0.09→0.6，以原 E4 与本次中较高完整配置为父实验，固定该父实验的 lr，从头训，不接 checkpoint 改温度。第二刀后本轮收口。

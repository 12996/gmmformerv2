10.2 首轮停训闸门用错了，应撤销：它不适用于 E3，也不能作为 CLIP–CLIP 训练的通用生死线；冻结 CLIP 的对齐体检与经过可学习映射、编码器后的训练验证不是同一对象，GMMFormer v2 的两端都包含需要训练的映射，不能要求首轮必然保留输入特征的检索能力。

目前只能判“前两轮尚未形成有效的验证集检索”，不能判失败原因：SumR 10.1→8.7 仍处于随机量级，loss 2.42→1.72 只说明训练目标下降，不证明正确语义对齐已经学会；“跨模态映射尚未学好”仍是合理解释，但这两点不足以定位到投影，也没有直接证据指向配对或 mask 错误。

E1 起步更快是警告，不是淘汰依据：E1 首轮 10.5 仅比该闸门高 0.3，没有证据证明 10.2 两侧对应不同的最终成败；E1 第二轮升至 14.7、E3 降至 8.7 值得保留记录，但不能据两轮强制要求不同特征配置具有相同学习速度，更不能拿 E2 的首轮 15.1 当 E3 必须达到的下限。

拍板选 A，不选 B 或 C：本次应记录为 “E3 被人工 SIGTERM 中断，结果未定”，而不是“换文本失败”；不把暂时最佳 10.1 当完整 E3 结果，不另开检查或下一变量，也不据此宣判 CLIP-B/32 天花板；CLIP／混合特征结果继续另表，禁止填入 Table 1 的 I3D 同设定栏。

下一动作：只取消首轮 ≤10.2 的人工停训规则，从头重跑同一 E3，按原 Early Stop 规则结束；固定 CLIP 视觉＋E1 RoBERTa、512／1024 输入、已修 TC、seed 9527、batch 128、损失、学习率及调度、HN≥20、max_es_cnt=10 和评估协议，不延长耐心值；以停止前最佳 SumR=S 判读：S≤68.0＝本设置与预算下换文本无收益；68.0<S≤78.2＝单 seed 改善但未冲线；78.2<S≤78.4＝仅数值超过论文；S>78.4＝单 seed 同时超过论文及本地基线，均不自动代表稳定提升或 SOTA。

# 工作状态（2026-09-16 E2 讨论 + git 快照）

E1 TC-only **已完成**（Early Stop，exit 0）。Best SumR **78.4** vs 论文 78.2 vs mean 基线 73.5。详见 `方向探索日志.md` E1。

E2 训练已派子 agent `01a0a863-cd15-7b83-976a-c82c4290c7a7`。E2-0 已通过。当时 5090_1 无空闲卡，子 agent 应等空卡再开 `tmp/clip_e2_seed9527`。

## 接着从哪继续

1. 看 `方向探索日志.md` 条目 **E2**（讨论结论 / 结果仍空，等父进程 GPT-6）。
2. E1 已结束，不必再等 `tmp/i3d_tc_seed9527` 开训。
3. SSH：`5090_1`（P6X8G / 202.207.1.21），项目 `/data/zhaopu/wang-2024-gmmformer-v2`
4. 在 GPT-6 定闸门之前：**不要**抽 CLIP 特征、**不要** `main.py -d cha` CLIP 重训。

## 已完成

- CLIP 线作废（视觉 A3PRVR `clip_feat` 512-d；文本 open_clip `ln_final` 无 `text_projection`）。冻结 Rsum **8.55**（随机）；训练后 seed9527 Rsum 9.6。
- I3D+自抽 RoBERTa mean 版：seed 9527 Best **2.4 / 7.6 / 13.3 / 50.2 / SumR 73.5**。日志 `tmp/i3d_seed9527/.../log.txt`。**不要覆盖这个目录。**
- E1 只删 `DyGMMBlock` 的 `torch.mean(oo)` 覆盖后重训：Best **2.4 / 8.8 / 14.2 / 53.0 / SumR 78.4**（epoch 34）。日志 `tmp/i3d_tc_seed9527/.../log.txt`。
- GPT-6 Pro（E1）：https://chatgpt.com/c/6aa970a5-dabc-83ea-bb21-0f02d186963a
- `AGENTS.md` 方向探索：GPT-6 定方向 → 子 agent 实验 → 主 agent 写 `方向探索日志.md`
- E1 补丁已上：`vendor/GMMFormer_v2/src/Models/gmmformerV2/model_components.py` 删掉 `out = torch.mean(oo, dim = -1).squeeze()`。备份：`model_components.py.bak_e1_mean_20260916`

## 未完成

- E2 仍在讨论，**没有** CLIP 开训。
- 草稿闸门（GPT-6 可改）：重抽带 `text_projection` 的 CLIP 文本 → 冻结检索必须明显高于 ~9 否则停 → 再训 GMMFormer v2 TC-fixed seed 9527。
- CLIP 结果不得与 I3D Table 1 混报。超过论文 78.2 ≠ 超过 DreamPRVR 80.0 / A3PRVR 82.3。

## 机器核对命令（E1 归档；E2 未开训）

```bash
ssh 5090_1
grep -n "torch.mean(oo" /data/zhaopu/wang-2024-gmmformer-v2/vendor/GMMFormer_v2/src/Models/gmmformerV2/model_components.py
ps -u zhaopu -o pid,etime,cmd | grep "main.py -d cha"
tail -20 /data/zhaopu/wang-2024-gmmformer-v2/tmp/i3d_tc_seed9527/results/charades/gmmformer_v2/log.txt
nvidia-smi
```
